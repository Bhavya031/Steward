Vision: verified local work→model-free recipes.
Locked: Bun/TS; macOS arm64/x64; Svelte P3; binaries `ffmpeg ffprobe pandoc magick ocrmypdf whisper-cli gs soffice brew`; step helpers `ls cat mkdir cp mv stat du head tail`. Final lists; localhost token.
Now: P2.10 four recipes green; Recipe 4 planned fresh and reran a different file with zero model calls. Two invalid-check runs captured before semantic target validation forced the existing defensive re-ask. Await approval; Step 11 next. `server/` is CLI-only; HTTP/WS arrives P3.
Rules: ≤150L; heavy ask; `gpt-5.6-sol`; executor-only spawn; recipes≠agent; argv[][]; schema frozen; checks are target-validated before execution; repair≤3/evidence-only/fixed checks; pre-spawn rejections consume/log attempts while retaining measured evidence; outputs confined; ordinary intermediates must be declared direct `{{temp_dir}}` children, become readable only after write, and always clean; argv positively classified; failed outputs removed; recipe=authored plan; non-path slots require serialized model-declared derivations; baked loudnorm measurements cannot save; match ≥0.45 + lead ≥0.15.
Risks: Brew `/opt/homebrew|/usr/local`; isolate `soffice`; passlogs/null sinks are executor-owned temp artifacts; shared loudness scan/file/run; ≤-69 LUFS=silent fail; PDF parse/text=GS only; ZIP markers; Day5 deferrals in AUDIT.md.
