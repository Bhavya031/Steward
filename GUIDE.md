Vision: verified local work→model-free recipes.
Locked: Bun/TS; macOS arm64/x64; Svelte P3; binaries `ffmpeg ffprobe pandoc magick ocrmypdf whisper-cli gs soffice brew`; step helpers `ls cat mkdir cp mv stat du head tail`. Final lists; localhost token.
Now: P2.9.5 security gate done; await 10. `server/` is CLI-only; HTTP/WS arrives in P3.
Rules: ≤150L; heavy ask; `gpt-5.6-sol`; executor-only spawn; recipes≠agent; argv[][]; repair≤3/evidence-only/fixed checks; schema=current verifiers; outputs confined; argv positively classified per tool; failed outputs removed.
Risks: Brew `/opt/homebrew|/usr/local`; isolate `soffice`; passlogs in `{{temp_dir}}`; loudnorm JSON is stderr; one scan/file/run; ≤-69 LUFS=silent fail; PDF parse/text=GS only; ZIP structural markers; Day 5 deferrals in AUDIT.md.
