Vision: verified local work→model-free recipes.
Locked: Bun/TS; macOS arm64/x64; Svelte P3; binaries `ffmpeg ffprobe pandoc magick ocrmypdf whisper-cli gs soffice brew`; step helpers `ls cat mkdir cp mv stat du head tail`. Final lists; localhost token.
Now: P2.9 done; await 10.
Rules: ≤150L; heavy ask; `gpt-5.6-sol`; executor-only spawn; recipes≠agent; argv[][]; repair≤3/evidence-only/fixed checks; schema=current verifiers.
Risks: Brew `/opt/homebrew|/usr/local`; isolate `soffice`; passlogs in `{{temp_dir}}`; loudnorm JSON is stderr; one scan/file/run; ≤-69 LUFS=silent fail; PDF parse/text=GS only; ZIP formats require structural markers.
