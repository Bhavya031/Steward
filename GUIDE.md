Vision: verified local work→model-free recipes.
Locked: Bun/TS; macOS arm64/x64; Svelte P3; binaries `ffmpeg ffprobe pandoc magick ocrmypdf whisper-cli gs soffice brew`; step helpers `ls cat mkdir cp mv stat du head tail`. Final lists; localhost token.
Now: P2.10 recipe 3 green; await recipe 4. `server/` is CLI-only; HTTP/WS arrives in P3.
Rules: ≤150L; heavy ask; `gpt-5.6-sol`; executor-only spawn; recipes≠agent; argv[][]; repair≤3/evidence-only/fixed checks; schema=current verifiers; outputs confined; argv positively classified per tool; failed outputs removed; runtime media/loudness slots resolve locally; recipe match needs capability intent, ≥0.45 score, ≥0.15 lead.
Risks: Brew `/opt/homebrew|/usr/local`; isolate `soffice`; passlogs in `{{temp_dir}}`; loudnorm recipe measures each input then applies measured fields; verification shares one scan/file/run; ≤-69 LUFS=silent fail; PDF parse/text=GS only; ZIP structural markers; media profiles fixed; MP4/MOV use brand and WebM/MKV use EBML evidence; Day 5 deferrals in AUDIT.md.
