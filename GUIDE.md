# Steward Guide

Vision: Local → model-free recipes.

Locked: Bun/TS, macOS arm64/x64, Svelte P3. Tools: `ffmpeg ffprobe pandoc magick ocrmypdf whisper-cli gs soffice brew`; helpers: `ls cat mkdir cp mv stat du head tail` (steps only). No additions. Localhost+token; green-only evidence.

Now: P1 complete. P2 demo: `-fs` truncation → duration fail → two-pass repair (needs multi-command plan).

Rules: ≤150 lines; heavy=yes; `gpt-5.6-sol`; executor-only spawns; agent-free recipes.

Risks: Brew `/opt/homebrew` arm64, `/usr/local` x64; isolate `soffice`.
