Vision: verified local work→model-free recipes.
Locked: Bun/TS; macOS arm64/x64; Svelte P3; binaries `ffmpeg ffprobe pandoc magick ocrmypdf whisper-cli gs soffice brew`; step helpers `ls cat mkdir cp mv stat du head tail`. Final lists; localhost token.
Now: P2.10 hero compression is honest: fresh 21s run + model-authored derivation green; 37s recipe rerun green/model calls 0. Await approval. `server/` is CLI-only; HTTP/WS arrives in P3.
Rules: ≤150L; heavy ask; `gpt-5.6-sol`; executor-only spawn; recipes≠agent; argv[][]; repair≤3/evidence-only/fixed checks; outputs confined; argv positively classified; failed outputs removed; recipe=authored plan; slots never add hidden behavior; every non-path command slot names a model-declared closed/typed derivation stored verbatim; baked loudnorm measurements cannot save; match needs ≥0.45 score and ≥0.15 lead.
Risks: Brew `/opt/homebrew|/usr/local`; isolate `soffice`; passlogs in `{{temp_dir}}`; repair plans cannot write intermediate media in temp (an honest 8s repair hit this policy); shared loudness scan/file/run; ≤-69 LUFS=silent fail; PDF parse/text=GS only; ZIP markers; Day5 deferrals in AUDIT.md.
